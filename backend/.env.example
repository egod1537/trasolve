# Copy to backend/.env.local. Use server keys restricted to their respective APIs.
# Do not use the browser key restricted by HTTP referrers.
# Routes API
GOOGLE_ROUTES_API_KEY=
# Places API (New). Never expose this key through a VITE_ variable.
GOOGLE_PLACES_API_KEY=

# Absolute path, or relative to the backend directory. Defaults to backend/data.
TRASOLVE_DATA_DIR=
# Temporary current user, resolved by the backend (no authentication yet).
TRASOLVE_LOCAL_USER_ID=local-user
